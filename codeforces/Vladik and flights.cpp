#include<bits/stdc++.h>
#define ll long long int
#define mi map<int,int>
#define mcr map<char,int>
using namespace std;

int main()
{
    ll n, k, m, i, a, b, z, o, mx;
    while(cin >> n >> a >> b)
    {
        string s;
        cin >> s;
        z = 0, o = 0;
        if(s[a - 1] == s[b - 1])
        {
            cout << 0 << endl;

        }else{
            cout << 1 << endl;
        }


    }
    return 0;
}
