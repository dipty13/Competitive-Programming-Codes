#include<bits/stdc++.h>
#define ll long long int
#define mi map<int,int>
#define mcr map<char,int>
using namespace std;
int main()
{
    ll n, t, k, m, a, b, c;
    fin >> n;
    vector<ll> v(n);
    for(ll i = 0; i < n; i++)
    {
        fin >> v[i];
    }
    sort(v.begin(), v.end());
    return 0;
}
