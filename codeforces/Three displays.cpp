#include<bits/stdc++.h>
using namespace std;
int main()
{
    int i,j,n;
    while(cin>>n)
    {
        long long int int s[n+5],c[n+5],dp[n+5];
        memset(dp,0,sizeof(dp));
        dp[0] = 1;

        for(i=1;i<=n;i++)
        {
            cin<<s[i];
        }

    }
    return 0;
}

