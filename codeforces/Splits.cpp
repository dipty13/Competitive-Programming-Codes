#include<bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
    ll n,x;
    while(cin>>n)
    {
        vector<ll> v(n + 5);
        for(int i = 0; i < n; i++){
            cin >> v[i];
        }
        for(int i = 1; i < n; i++){

        }
    }
    return 0;
}
