#include<bits/stdc++.h>
#define ll long long int
#define mi map<int,int>
#define mcr map<char,int>
using namespace std;
void solution(ll a, ll b, ll c, ll d)
{
    cout << b << " " << c << " " << c << endl;
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    ll n,i, a,b,c,d, t;
    cin >> t;
    while(t--)
    {
       cin >> a >> b >> c >> d;
       solution(a,b,c,d);
    }

    return 0;
}

