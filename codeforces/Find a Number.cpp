#include<bits/stdc++.h>
#define ll long long int
#define mi map<int,int>
#define mcr map<char,int>
using namespace std;
ll sumDigits(int  n)
	{
        int sum = 0;
        while (num1 != 0)
		{
            sum += num1 % 10;
            num1 /= 10;
        }
        return sum;
    }
int main()
{
    ll d, s, m, c;
    while(cin >> d >> s)
    {
        for(i = d * 2; ; i++)
        {
            if(i % d == 0 &&)
        }
    }
    return 0;
}
