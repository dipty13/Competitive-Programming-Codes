#include<bits/stdc++.h>
#define ll long long int
#define mi map<int,int>
#define mcr map<char,int>
using namespace std;
int main()
{
    ll n, t, k, m, a, b, c, i;
    cin >> t;
    while(t--)
    {
        cin >> n >> m;
        vector<string> v(n * m);
        map<ll, ll> m;
        ll odd = 0, even = 0;
        for(i = 1; i <= n; i++){
            cin >> v[i];
        }
        a = n, b = m
        while(i < n * m){
            while(a != 1 && b != 1){
                if(v[a][])
            }
        }
        cout << endl;
    }
    return 0;
}
